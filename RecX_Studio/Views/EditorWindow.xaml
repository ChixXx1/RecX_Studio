<Window x:Class="RecX_Studio.Views.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utils="clr-namespace:RecX_Studio.Utils"
        mc:Ignorable="d"
        Title="Редактор видео" 
        Height="600" 
        Width="800"
        MinHeight="400"
        MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Background="#252526"
        Foreground="White">
    
    <Window.Resources>
        <!-- ... (существующие стили без изменений) ... -->
        
        <!-- Существующие стили -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#666666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4C4C4C"/>
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Width" Value="80"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Стили для ошибок и конвертер (без изменений) -->
        <Style TargetType="TextBlock" x:Key="ErrorTextStyle">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="5,2,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="MaxWidth" Value="150"/>
        </Style>
        <utils:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#3E3E3E" BorderThickness="1" Background="Black">
            <MediaElement x:Name="PlayerMediaElement" 
                          LoadedBehavior="Manual"
                          UnloadedBehavior="Stop"
                          Stretch="Uniform"/>
        </Border>
        
        <Border Grid.Row="1" Background="#2D2D30" Padding="12" Margin="0,10,0,0" CornerRadius="3">
            <StackPanel>
                <!-- Панель информации о времени -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock Text="Длительность:"/>
                    <TextBlock Text="{Binding DurationFormatted}" FontWeight="Bold"/>
                </StackPanel>
                
                <!-- Панель ввода времени обрезки -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Начало:"/>
                        <TextBox x:Name="StartTimeTextBox" 
                                 Text="{Binding StartTimeText, UpdateSourceTrigger=PropertyChanged}"
                                 utils:TimeInputBehavior.IsEnabled="True" />
                        <TextBlock Text="{Binding StartTimeError}" 
                                   Style="{StaticResource ErrorTextStyle}" 
                                   Visibility="{Binding StartTimeError, Converter={StaticResource NullToVisibilityConverter}}"/>
                    </StackPanel>
                    
                    <Button Content="Установить" Command="{Binding SetStartCommand}" Margin="10,0,0,0"/>
                    
                    <StackPanel Margin="20,0,0,0">
                        <TextBlock Text="Конец:"/>
                        <TextBox x:Name="EndTimeTextBox" 
                                 Text="{Binding EndTimeText, UpdateSourceTrigger=PropertyChanged}"
                                 utils:TimeInputBehavior.IsEnabled="True" />
                        <TextBlock Text="{Binding EndTimeError}" 
                                   Style="{StaticResource ErrorTextStyle}" 
                                   Visibility="{Binding EndTimeError, Converter={StaticResource NullToVisibilityConverter}}"/>
                    </StackPanel>
                    
                    <Button Content="Установить" Command="{Binding SetEndCommand}" Margin="10,0,0,0"/>
                    
                    <Button Content="Сбросить" Command="{Binding ResetCommand}" Margin="20,0,0,0"/>
                </StackPanel>

                <!-- НОВОЕ: Памятка по вводу времени -->
                <Border Background="#3E3E3E" CornerRadius="3" Padding="10" Margin="0,0,0,10">
                    <TextBlock FontSize="11" LineHeight="16" TextWrapping="Wrap">
                        <Run Text="💡 Как установить время:"/><LineBreak/>
                        <Run Text="  1. Просто введите последовательность цифр (напр., 123 для 00:01:23, 023 для 00:00:23)."/><LineBreak/>
                        <Run Text="  2. Поле отформатируется автоматически после короткой паузы в вводе."/><LineBreak/>
                        <Run Text="  3. Чтобы исправить ошибку, используйте Backspace и введите цифры заново."/><LineBreak/>
                        <Run Text="  4. Красные сообщения об ошибках под полями покажут, если время некорректно."/>
                    </TextBlock>
                </Border>
                
                <!-- Панель управления воспроизведением и сохранением -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="▶ Воспроизвести" Command="{Binding PlayPauseCommand}"/>
                    <Button Content="⏹ Стоп" Command="{Binding StopCommand}"/>
                    <Separator Width="20"/>
                    <Button Content="💾 Сохранить отрезок" Command="{Binding SaveCommand}" Background="#007ACC"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>