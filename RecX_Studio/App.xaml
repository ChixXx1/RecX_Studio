<Application x:Class="RecX_Studio.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RecX_Studio"
             xmlns:utils="clr-namespace:RecX_Studio.Utils"
             StartupUri="MainWindow.xaml">
    
    
    <Application.Resources>
        <utils:PauseButtonTextConverter x:Key="PauseButtonTextConverter"/>
        <utils:PauseButtonStyleConverter x:Key="PauseButtonStyleConverter"/>
        <utils:StopButtonStyleConverter x:Key="StopButtonStyleConverter"/>
        <utils:ZoomConverter x:Key="ZoomConverter"/>
        <utils:EmptyImageConverter x:Key="EmptyImageConverter"/>
        
        <!-- Единый шаблон для всех кнопок с эффектами наведения -->
        <ControlTemplate x:Key="ButtonControlTemplate" TargetType="Button">
            <Border x:Name="border" 
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8"
                    Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#4E4E52"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#5E5E62"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        
        <!-- Общий стиль для кнопок, использующий единый шаблон -->
        <Style x:Key="ControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template" Value="{StaticResource ButtonControlTemplate}"/>
        </Style>
        
        <!-- Стиль для кнопок масштабирования, также использующий единый шаблон -->
        <Style x:Key="ZoomButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template" Value="{StaticResource ButtonControlTemplate}"/>
        </Style>

        <Style x:Key="DisabledButtonStyle" TargetType="Button" BasedOn="{StaticResource ControlButtonStyle}">
            <Setter Property="Opacity" Value="0.5"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="IsEnabled" Value="False"/>
        </Style>
         
    </Application.Resources>
</Application>