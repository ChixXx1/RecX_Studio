<UserControl x:Class="RecX_Studio.Views.StatusBarPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RecX_Studio.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="1200" 
             Height="25">
    
    
    <StatusBar Background="#2D2D30" Foreground="White">
        <!-- Текущее время -->
        <StatusBarItem>
            <StackPanel Orientation="Horizontal" Margin="5,0">
                <TextBlock Text="🕒" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding StatusInfo.CurrentTime}" 
                           FontFamily="Consolas" 
                           FontSize="12"/>
            </StackPanel>
        </StatusBarItem>
        
        <Separator Background="#5E5E5E" Margin="5,0" Width="1"/>
        
        <!-- Время записи -->
        <StatusBarItem>
            <StackPanel Orientation="Horizontal" Margin="5,0">
                <TextBlock Text="⏺" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding StatusInfo.RecordingTime}" 
                           FontFamily="Consolas" 
                           FontSize="12"/>
            </StackPanel>
        </StatusBarItem>
        
        <Separator Background="#5E5E5E" Margin="5,0" Width="1"/>
        
        <!-- CPU usage -->
        <StatusBarItem>
            <StackPanel Orientation="Horizontal" Margin="5,0">
                <TextBlock Text="⚡" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding StatusInfo.CpuUsage}" 
                           FontFamily="Consolas" 
                           FontSize="12"/>
            </StackPanel>
        </StatusBarItem>
        
        <Separator Background="#5E5E5E" Margin="5,0" Width="1"/>
        
        <!-- FPS -->
        <StatusBarItem>
            <StackPanel Orientation="Horizontal" Margin="5,0">
                <TextBlock Text="🎮" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding StatusInfo.Fps}" 
                           FontFamily="Consolas" 
                           FontSize="12"/>
            </StackPanel>
        </StatusBarItem>
    </StatusBar>
</UserControl>
