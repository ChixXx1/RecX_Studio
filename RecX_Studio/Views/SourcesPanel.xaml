<UserControl x:Class="RecX_Studio.Views.SourcesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RecX_Studio.Views"
             xmlns:vm="clr-namespace:RecX_Studio.ViewModels"
             xmlns:converters="clr-namespace:RecX_Studio.Utils"
             xmlns:utils="clr-namespace:RecX_Studio.Utils"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500"
             MinHeight="120" MinWidth="400"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    
    <UserControl.Resources>
        <!-- Конвертер для преобразования null в false -->
        <utils:NullToFalseConverter x:Key="NullToFalseConverter"/>

        <!-- Обновленный стиль для элемента источника -->
        <Style x:Key="SourceItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <!-- Эффект наведения на весь элемент -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#353538"/>
                </Trigger>
                <!-- Выделенный элемент -->
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="#1E3A5F"/>
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                </DataTrigger>
                 <!-- Неактивный элемент -->
                <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                    <Setter Property="Opacity" Value="0.6"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Border Background="#252526" CornerRadius="5" Margin="0, 0.8, 0, 0.8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Background="#2D2D30" CornerRadius="3,3,0,0" Padding="5,2">
                <TextBlock Text="Источники" 
                          Foreground="White"
                          FontWeight="Bold"
                          VerticalAlignment="Center"/>
            </Border>

            <!-- Прокручиваемый список источников с новым шаблоном -->
            <ScrollViewer Grid.Row="1" 
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Padding="2">
                <ItemsControl ItemsSource="{Binding Sources}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SourceItemStyle}"
                                    MouseLeftButtonDown="SourceItem_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Иконка типа источника -->
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="18" Margin="0,0,8,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="❓"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="ScreenCapture">
                                                        <Setter Property="Text" Value="🖥️"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="WindowCapture">
                                                        <Setter Property="Text" Value="🪟"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="AreaCapture">
                                                        <Setter Property="Text" Value="✂️"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    
                                    <!-- Название источника -->
                                    <TextBlock Grid.Column="1" 
                                              Text="{Binding Name}" 
                                              Foreground="White"
                                              VerticalAlignment="Center"
                                              TextTrimming="CharacterEllipsis"
                                              Margin="0,0,5,0"/>

                                    <!-- Кнопка удаления (появляется при наведении) -->
                                    <Button Grid.Column="2" 
                                            Name="DeleteButton"
                                            Content="✕" 
                                            ToolTip="Удалить источник"
                                            Width="24"
                                            Height="24"
                                            Margin="0"
                                            Padding="0"
                                            Click="RemoveSource_Click"
                                            Tag="{Binding}">
                                        <!-- ИСПРАВЛЕНО: Убран атрибут Style, оставлен только блок Button.Style -->
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource ZoomButtonStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <!-- Кнопка смотрит на IsMouseOver родительского Border -->
                                                    <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Нижняя панель с кнопками управления -->
            <Border Grid.Row="2" Background="#2D2D30" CornerRadius="0,0,3,3" Padding="5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- Кнопка добавления -->
                    <Button Content="+" 
                            ToolTip="Добавить источник"
                            Width="26"
                            Height="26"
                            Margin="2, 2, 10, 2"
                            Style="{StaticResource ZoomButtonStyle}"
                            Click="AddSource_Click"/>
                    
                    <!-- Кнопка перемещения вверх -->
                    <Button Content="↑" 
                            ToolTip="Переместить выбранный источник вверх"
                            Width="26"
                            Height="26"
                            Margin="0, 2, 10, 2"
                            Style="{StaticResource ZoomButtonStyle}"
                            Click="MoveSourceUp_Click"
                            IsEnabled="{Binding CanMoveUp}"/>
                    
                    <!-- Кнопка перемещения вниз -->
                    <Button Content="↓" 
                            ToolTip="Переместить выбранный источник вниз"
                            Width="26"
                            Height="26"
                            Margin="0, 2, 10, 2"
                            Style="{StaticResource ZoomButtonStyle}"
                            Click="MoveSourceDown_Click"
                            IsEnabled="{Binding CanMoveDown}"/>
                    
                    <!-- Кнопка удаления выбранного источника -->
                    <Button Content="×" 
                            ToolTip="Удалить выбранный источник"
                            Width="26"
                            Height="26"
                            Margin="0, 2, 10, 2"
                            Style="{StaticResource ZoomButtonStyle}"
                            Click="RemoveSelectedSource_Click"
                            IsEnabled="{Binding SelectedSource, Converter={StaticResource NullToFalseConverter}}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>